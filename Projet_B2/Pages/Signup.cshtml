@page
@* FONCTIONNALITE: inscription utilisateur et creation du compte applicatif. *@
@{ Layout = "_Layout"; ViewData["Title"] = "Inscription"; }

<div class="d-flex justify-content-center align-items-center" style="min-height:65vh">
  <div class="card shadow auth-card" style="max-width:440px;width:100%">
    <div class="card-body p-4">
      <div class="text-center mb-4">
        <img src="~/images/Logo_1_mini.svg" asp-append-version="true" alt="Laceris Tech" style="height:48px" class="mb-2" />
        <h4 class="fw-bold mt-2">Créer un compte</h4>
        <p class="text-muted small mb-0">Réservez votre premier espace en quelques secondes.</p>
      </div>

      <form id="signupForm">
        <div class="mb-3">
          <label class="form-label fw-medium">Nom complet</label>
          <div class="input-group">
            <span class="input-group-text"><i class="bi bi-person"></i></span>
            <input name="name" class="form-control" required placeholder="Jane Doe" />
          </div>
        </div>
        <div class="mb-3">
          <label class="form-label fw-medium">Email</label>
          <div class="input-group">
            <span class="input-group-text"><i class="bi bi-envelope"></i></span>
            <input name="email" type="email" class="form-control" required placeholder="you@example.com" />
          </div>
        </div>
        <div class="mb-3">
          <label class="form-label fw-medium">Mot de passe</label>
          <div class="input-group">
            <span class="input-group-text"><i class="bi bi-lock"></i></span>
            <input name="password" type="password" class="form-control" required minlength="6" placeholder="Au moins 6 caractères" />
          </div>
        </div>
        <div id="msg" class="text-danger small mb-2"></div>
        <button type="submit" class="btn btn-primary w-100">
          <i class="bi bi-person-plus me-1"></i>Créer le compte
        </button>
        <div class="text-center mt-3 text-muted small">
          Déjà inscrit ? <a href="/Login">Se connecter</a>
        </div>
      </form>
    </div>
  </div>
</div>

<script>
document.getElementById('signupForm').addEventListener('submit', async (e) => {
  e.preventDefault();
  const fd = new FormData(e.target);
  const msg = document.getElementById('msg');
  msg.textContent = '';
  const res = await fetch('/api/signup', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ name: fd.get('name'), email: fd.get('email'), password: fd.get('password') }),
    credentials: 'include'
  });
  if (res.ok) location.href = '/Booking';
  else {
    const j = await res.json().catch(()=>null);
    msg.textContent = j?.error || 'Inscription impossible';
  }
});
</script>
